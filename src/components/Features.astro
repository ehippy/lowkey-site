---
// Features component to showcase lowkey's key features
---

<section class="features" id="features">
	<div class="container">
		<div class="features-header">
			<h2 class="features-title text-center text-balance">
				Thoughtfulness, simplified
			</h2>
			<p class="features-subtitle text-center text-balance">
				lowkey helps you build the habit of showing up in creative, meaningful ways.
			</p>
		</div>
		
		<div class="features-grid">
			<div class="feature-card">
				<h3 class="feature-title">🎯 Just for you</h3>
				<p class="feature-description">
					lowkey is your personal advisor. No couple’s workbook. No shared logins. Just clever nudges to help you show up.
				</p>
			</div>

			<div class="feature-card">
				<h3 class="feature-title">💝 Personal Touch</h3>
				<p class="feature-description">
					Suggestions tailored to your partner's personality, interests, and love language. Every nudge feels personal and authentic.
				</p>
			</div>

			<div class="feature-card">
				<h3 class="feature-title">⏰ Perfect Timing</h3>
				<p class="feature-description">
					Smart scheduling that respects your routine. Suggestions arrive when you can actually act on them, not when it's inconvenient.
				</p>
			</div>

			<div class="feature-card">
				<h3 class="feature-title">✨ Small Gestures</h3>
				<p class="feature-description">
					Focus on simple, achievable actions. Send a thoughtful text, leave a note, or share something that reminded you of them.
				</p>
			</div>

			<div class="feature-card">
				<h3 class="feature-title">💭 Reflection Space</h3>
				<p class="feature-description">
					A private space to track your thoughtful moments and reflect on your relationship growth. No social pressure, just personal insight.
				</p>
			</div>

			<div class="feature-card">
				<h3 class="feature-title">🔒 Privacy First</h3>
				<p class="feature-description">
					Your personal data is encrypted and protected. We collect only what's needed to personalize your experience. No social features, no data selling.
				</p>
			</div>
		</div>
	</div>
</section>

<style>
	.features {
		padding: 6rem 0;
		background-color: var(--color-surface);
	}

	.features-header {
		text-align: center;
		margin-bottom: 4rem;
		max-width: 600px;
		margin-left: auto;
		margin-right: auto;
	}

	.features-title {
		font-size: 2.5rem;
		font-weight: 600;
		margin-bottom: 1rem;
		color: var(--color-text);
	}

	.features-subtitle {
		font-size: 1.25rem;
		color: var(--color-text-light);
	}

	.features-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
		gap: 2rem;
		max-width: 1000px;
		margin: 0 auto;
	}

	.feature-card {
		background: var(--color-surface);
		border: 1px solid var(--color-border);
		border-radius: var(--border-radius);
		padding: 2rem;
		text-align: center;
		transition: all 0.3s ease;
		box-shadow: var(--shadow-soft);
	}

	.feature-card:hover {
		transform: translateY(-4px);
		box-shadow: var(--shadow-medium);
		border-color: var(--color-warm);
	}

	.feature-icon {
		display: inline-flex;
		align-items: center;
		justify-content: center;
		width: 64px;
		height: 64px;
		background: var(--gradient-icon);
		border-radius: 50%;
		margin-bottom: 1.5rem;
		color: white;
	}

	.feature-icon.accent {
		background: var(--gradient-icon-accent);
	}

	.feature-title {
		font-size: 1.25rem;
		font-weight: 600;
		margin-bottom: 1rem;
		color: var(--color-text);
	}

	.feature-description {
		color: var(--color-text-light);
		line-height: 1.6;
	}

	@media (max-width: var(--breakpoint-mobile)) {
		.features {
			padding: 4rem 0;
		}

		.features-title {
			font-size: 2rem;
		}

		.features-subtitle {
			font-size: 1.1rem;
		}

		.features-grid {
			grid-template-columns: 1fr;
			gap: 1.5rem;
		}

		.feature-card {
			padding: 1.5rem;
		}

		.feature-icon {
			width: 56px;
			height: 56px;
		}
	}
</style>
